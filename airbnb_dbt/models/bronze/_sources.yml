version: 2

sources:
  - name: raw
    schema: main
    meta:
      external_location: "read_csv_auto('../data/{name}.csv')"
    tables:
      - name: hosts
      - name: listings
      - name: reviews
